version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - npm install
  pre_build:
    commands:
      - aws2 s3 cp s3://config.grimpunt.be/environment.beta.ts src/environments/environment.ts
      - aws2 s3 cp s3://config.grimpunt.be/.beta-env
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - npm run deploy
cache:
  paths:
    - node_modules
